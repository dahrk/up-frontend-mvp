{
  "name": "up-frontend-mvp",
  "version": "0.1.0",
  "dependencies": {
    "@notionhq/client": "^0.1.8",
    "dotenv": "^10.0.0",
    "express": "^4.17.1"
  },
  "scripts": {
    "client": "cd client && yarn start",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "devDependencies": {
    "concurrently": "^6.2.0"
  }
}
